### DATABASE ###
spring.datasource.url=jdbc:mysql://localhost:3306/KnowlegeBridge?useUnicode=true&characterEncoding=UTF-8&characterSetResults=utf8&createDatabaseIfNotExist=true&useSSL=false&useLegacyDatetimeCode=false&serverTimezone=UTC&useInnoDB=true
spring.datasource.username=root
spring.datasource.password=

### JPA / HIBERNATE ###
spring.jpa.show-sql=true
spring.jpa.hibernate.ddl-auto=update
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL5Dialect
spring.jpa.properties.hibernate.dialect.storage_engine=innodb

### Server Port and Context Path ###
server.port=8010
server.servlet.context-path=/SpringMVC

### Multipart File Upload Configuration ###
spring.servlet.multipart.max-file-size=100MB
spring.servlet.multipart.max-request-size=200MB

#Web configuration
spring.mvc.view.prefix=/views/
spring.mvc.view.suffix=.jsp

max_allowed_packet=820M



# Spring Boot already provides default configurations for multipart file upload,
# but you can customize them if needed.
# MULTIPART (MultipartProperties)
# Enable multipart uploads
# Activation de la gestion des fichiers multipart

# Uncomment and configure the following properties if needed for email:
# spring.mail.host=smtp.gmail.com
# spring.mail.port=587
# spring.mail.username=mahmoud.lakhal@esprit.tn
# spring.mail.password=motdepasse
# spring.mail.properties.mail.smtp.auth=true
# spring.mail.properties.mail.smtp.starttls.enable=true
